services:
  backend:
    build:
      context: ./backend
    ports:
      - "3003:3003"

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_BASE_URL: "http://localhost:3003"
    ports:
      - "3002:3002"
    depends_on:
      - backend
